---
- name: Add the group tomcat
  group:
    name: "{{ tomcat_user }}"
    gid: '{{ tomcat_uid }}'

- name: Add the user tomcat
  user:
    name: '{{ tomcat_user }}'
    comment: '{{ tomcat_user }}'
    uid: '{{ tomcat_uid }}'
    group: '{{ tomcat_user }}'
    home: '{{ tomcat_path }}'

- name: Configure BASH profile
  lineinfile:
    dest: '{{ tomcat_path }}/.bash_profile'
    state: present
    line: '{{ item }}'
  with_items:
    - '{{ tomcat_environment }}'

- name: Install Apache Tomcat {{ tomcat_version }} Core
  unarchive:
    src: '{{ tomcat_soft_url }}{{ tomcat_version[0:1] }}/v{{ tomcat_version }}/bin/apache-tomcat-{{ tomcat_version }}.tar.gz'
    dest: '{{ tomcat_path }}'
    owner: '{{ tomcat_user }}'
    group: '{{ tomcat_user }}'
    exclude: '{{ tomcat_exclusions }}'
    extra_opts: [--strip-components=1]
    remote_src: yes
  register: tomcat_update

- name: Install Apache Tomcat {{ tomcat_version }} Extras
  get_url:
    url: '{{ tomcat_soft_url }}{{ tomcat_version[0:1] }}/v{{ tomcat_version }}/bin/extras/{{ item.src }}'
    dest: '{{ item.dest }}'
    owner: '{{ tomcat_user }}'
    group: '{{ tomcat_user }}'
  with_items:
    - { src: 'catalina-jmx-remote.jar',  dest: '{{ tomcat_path }}/lib/' }
    - { src: 'tomcat-juli-adapters.jar', dest: '{{ tomcat_path }}/lib/', create: "{{ tomcat_version[0:3] == '8.0' }}" }
    - { src: 'tomcat-juli.jar',          dest: '{{ tomcat_path }}/bin/', create: "{{ tomcat_version[0:3] == '8.0' }}" }
  when: item.create | default(true) | bool
  register: tomcat_upgrade

- name: Install support binary package
  copy:
    src: '{{ item }}'
    dest: '{{ tomcat_path }}/bin/'
    owner: '{{ tomcat_user }}'
    group: '{{ tomcat_user }}'
    mode: 0750
  with_items:
    - '{{ support_binary }}'

- name: Install support JAR package
  copy:
    src: '{{ item }}'
    dest: '{{ tomcat_path }}/lib/'
    owner: '{{ tomcat_user }}'
    group: '{{ tomcat_user }}'
    mode: 0644
  with_items:
    - '{{ support_jars }}'
